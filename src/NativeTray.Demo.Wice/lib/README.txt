Place Wice.dll in this folder as `Wice.dll`.

This repository includes a small stub project `WiceStub` (under WiceStub/) that can be built locally to produce a test Wice.dll.

If you have the real Wice binary, replace the generated stub with the real `Wice.dll`.
