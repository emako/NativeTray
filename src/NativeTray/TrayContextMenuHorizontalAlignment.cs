namespace System.NativeTray;

/// <summary>
/// Controls how the tray context menu is horizontally aligned relative to the popup point.
/// </summary>
public enum TrayContextMenuHorizontalAlignment
{
    Left,
    Center,
    Right,
}