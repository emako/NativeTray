namespace System.NativeTray;

/// <summary>
/// Controls how the tray context menu is vertically aligned relative to the popup point.
/// </summary>
public enum TrayContextMenuVerticalAlignment
{
    Top,
    Center,
    Bottom,
}